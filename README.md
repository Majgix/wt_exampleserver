Client and Server with a Working Webtransport connection.

To test, first run the Server with:

```bash
cargo run
```
And then run the client in a separate terminal window:

```bash
cargo run --example client  
```